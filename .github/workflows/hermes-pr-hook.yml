name: Hermes PR Hook

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
      - converted_to_draft
      - labeled
      - unlabeled
      - edited

permissions:
  contents: read
  pull-requests: read

jobs:
  notify-hermes:
    if: ${{ github.event.pull_request.user.login == 'whatamelon' }}
    runs-on: ubuntu-latest
    steps:
      - name: Notify Hermes through Slack bot mention
        env:
          SLACK_BOT_TOKEN: ${{ secrets.HERMES_PR_HOOK_SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: C0B5SNVFT99
          BOT_USER_ID: U0B61PJTTN0
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          ACTION: ${{ github.event.action }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          LABEL_NAME: ${{ github.event.label.name || '' }}
        shell: bash
        run: |
          if [[ -z "$SLACK_BOT_TOKEN" ]]; then
            echo "Missing secret HERMES_PR_HOOK_SLACK_BOT_TOKEN; skipping Hermes notification" >&2
            exit 0
          fi
          TEXT="<@$BOT_USER_ID> github-pr-hook $REPO#$PR_NUMBER action=$ACTION sha=$HEAD_SHA label=$LABEL_NAME url=$PR_URL
$PR_TITLE"
          python3 - <<'PY' > /tmp/slack-payload.json
import json, os
print(json.dumps({"channel": os.environ["SLACK_CHANNEL"], "text": os.environ["TEXT"]}, ensure_ascii=False))
PY
          curl -fsS -X POST             -H "Authorization: Bearer $SLACK_BOT_TOKEN"             -H 'Content-Type: application/json; charset=utf-8'             --data @/tmp/slack-payload.json             https://slack.com/api/chat.postMessage
